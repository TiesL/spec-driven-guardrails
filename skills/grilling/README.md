# Attribution

Source: [`mattpocock/skills`](https://github.com/mattpocock/skills), path
`skills/productivity/grilling/`. MIT licensed — `LICENSE` in this folder is
the original, unmodified. Copied verbatim, not paraphrased, as a static
vendored copy — not a live dependency (no submodule, no package reference),
so upstream changes to that repo never reach this one without a deliberate
edit here.

`agents/openai.yaml` (OpenAI-harness display metadata, not relevant here)
was not vendored — only `SKILL.md`, the actual method.
