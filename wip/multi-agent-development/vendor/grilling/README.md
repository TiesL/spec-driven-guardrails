# Vendored: `grilling` skill

Source: [`mattpocock/skills`](https://github.com/mattpocock/skills), path
`skills/productivity/grilling/`, commit `85f83d3fde1d3a90d5c9a657f6998c79a6c37308`
(2026-08-20). MIT licensed — `LICENSE` in this folder is the original, unmodified.

`agents/openai.yaml` (OpenAI-harness display metadata, not relevant here) was not vendored —
only `SKILL.md`, the actual method.

Copied verbatim, not paraphrased, per the same reasoning as `../codebase-design/`: use the
actual design-tree/frontier/rounds method, not a lossy retelling of it. See
`ROLE-DESCRIPTIONS.md`'s Product section and `ARCHITECTURE-MULTI-AGENT-WIP.md`'s A10 for how
it's applied here.

**Also promoted to `skills/grilling/` (issue #291)** as a real, invokable skill for any
session in this repo — not gated on the multi-agent pipeline being built. This copy stays
here as the multi-agent design docs' own reference; the two are byte-identical copies of the
same upstream commit, not independently maintained. Update both, or neither, if either ever
changes.
